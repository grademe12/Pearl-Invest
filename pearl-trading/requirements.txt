# FastAPI 핵심
fastapi==0.104.1
uvicorn[standard]==0.24.0

# API 관련
pydantic==2.5.0
python-multipart==0.0.6
email_validator

# 모니터링
prometheus-client==0.19.0

# 환경변수 관리
python-dotenv==1.0.0

# 날짜/시간 처리
python-dateutil==2.8.2

# JSON 성능 향상 (선택사항)
orjson==3.9.10

# 비동기 HTTP 클라이언트 (서비스간 통신용)
httpx==0.25.2

# 데이터베이스 (필요시 추가)
# sqlalchemy==2.0.23
# databases==0.8.0

# 캐싱 (필요시 추가)
# redis==5.0.1

# 테스트 (개발용)
# pytest==7.4.3
# pytest-asyncio==0.21.1